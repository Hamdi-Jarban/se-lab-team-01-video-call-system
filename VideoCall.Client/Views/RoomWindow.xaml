<Window x:Class="VideoCall.Client.Views.RoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ÇáÛÑÝ ÇáÌãÇÚíÉ" Height="650" Width="900"
        MinHeight="560" MinWidth="760"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft">
    
    <!-- ÊÞÓíã ÇáæÇÌåÉ Åáì 4 ÃÌÒÇÁ ÑÆíÓíÉ: ÇáÊÑæíÓÉ¡ ÅÏÎÇá ÇáÛÑÝÉ¡ ÊÝÇÕíá ÇáÛÑÝÉ¡ æÔÑíØ ÇáÊÍßã -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ÇáÊÑæíÓÉ ÇáÚáæíÉ: ÚäÇæíä ÇáäÇÝÐÉ æÚÑÖ ÑÓÇÆá ÇáÍÇáÉ ÇáÚÇãÉ -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="ÇáÛÑÝ ÇáÌãÇÚíÉ" Style="{StaticResource TitleText}" />
                <TextBlock Text="ÃäÔÆ ÛÑÝÉ Ãæ ÇäÖã Åáì ãÍÇÏËÉ ÞÇÆãÉ"
                           Style="{StaticResource SubtitleText}" Margin="0,6,0,0" />
            </StackPanel>
            <Border Grid.Column="1" Background="#172554" CornerRadius="8" Padding="14,8">
                <TextBlock Text="{Binding StatusMessage}" Foreground="#BFDBFE" />
            </Border>
        </Grid>

        <!-- ÈØÇÞÉ ÇáÅäÔÇÁ æÇáÇäÖãÇã: ÊõÚØá ÇáÍÞæá ÊáÞÇÆíÇð ÈãÌÑÏ ÇáÏÎæá áÛÑÝÉ (ÇÑÊÈÇØ ÚßÓí ãÚ HasRoom) -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ãÚÑÝ ÇáÛÑÝÉ" Style="{StaticResource FieldLabel}" />
                    <TextBox Style="{StaticResource RoundedTextBox}"
                             Text="{Binding RoomId, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding HasRoom, Converter={StaticResource InverseBooleanConverter}}" />
                </StackPanel>
                <Button Grid.Column="1" Content="ÅäÔÇÁ ÛÑÝÉ" Style="{StaticResource PrimaryButton}"
                        Command="{Binding CreateRoomCommand}" Margin="12,20,4,0"
                        IsEnabled="{Binding HasRoom, Converter={StaticResource InverseBooleanConverter}}" />
                <Button Grid.Column="2" Content="ÇäÖãÇã" Style="{StaticResource SecondaryButton}"
                        Command="{Binding JoinRoomCommand}" Margin="4,20,0,0"
                        IsEnabled="{Binding HasRoom, Converter={StaticResource InverseBooleanConverter}}" />
            </Grid>
        </Border>

        <!-- ãäØÞÉ ÊÝÇÕíá ÇáÛÑÝÉ æÞÇÆãÉ ÇáÃÚÖÇÁ: áÇ ÊÙåÑ ÅáÇ ÅÐÇ ßÇä ÇáãÓÊÎÏã ÏÇÎá ÛÑÝÉ ÝÚáíÉ (Visibility ãÑÊÈØ ÈÜ HasRoom) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="285" />
            </Grid.ColumnDefinitions>

            <!-- ãÚáæãÇÊ ÇáÛÑÝÉ ÇáÍÇáíÉ æÇáãÖíÝ -->
            <Border Grid.Column="0" Background="#020617" CornerRadius="12" Padding="26"
                    Visibility="{Binding HasRoom, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding RoomId, StringFormat='ÇáÛÑÝÉ ÇáÍÇáíÉ: {0}'}"
                               FontSize="24" FontWeight="SemiBold" Foreground="#F8FAFC" />
                    <TextBlock Text="{Binding Host, StringFormat='ÇáãÖíÝ: {0}'}"
                               Foreground="#93C5FD" Margin="0,8,0,18" />
                    <TextBlock Text="{Binding IsMediaActive, StringFormat='ÍÇáÉ ÇáæÓÇÆØ: {0}'}"
                               Foreground="#CBD5E1" />
                    <TextBlock Text="íãßä ááãÖíÝ ÈÏÁ ÇáãÍÇÏËÉ ÈÚÏ ÇßÊãÇá ÞÇÆãÉ ÇáÃÚÖÇÁ."
                               Foreground="#64748B" TextWrapping="Wrap" Margin="0,12,0,0" />
                </StackPanel>
            </Border>

            <!-- ÇáÞÇÆãÉ ÇáÌÇäÈíÉ ááÃÚÖÇÁ æÅÖÇÝÉ ãÓÊÎÏãíä ÌÏÏ -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}"
                    Visibility="{Binding HasRoom, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="ÇáÃÚÖÇÁ" Style="{StaticResource FieldLabel}" />
                        <TextBlock Text="{Binding Members.Count, StringFormat=' ({0})'}" Foreground="#64748B" />
                    </DockPanel>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Members}" Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#334155" CornerRadius="7" Padding="10,8" Margin="0,3">
                                    <DockPanel>
                                        <Ellipse Width="8" Height="8" Fill="#22C55E" Margin="0,0,8,0" />
                                        <TextBlock Text="{Binding}" Foreground="#E2E8F0" />
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Grid.Row="2" Margin="0,14,0,0">
                        <TextBlock Text="ÅÖÇÝÉ ãÓÊÎÏã ãÊÕá" Style="{StaticResource FieldLabel}" />
                        <TextBox Style="{StaticResource RoundedTextBox}"
                                 Text="{Binding NewMemberUsername, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="ÅÖÇÝÉ ÚÖæ" Style="{StaticResource SecondaryButton}"
                                Command="{Binding AddUserCommand}" IsEnabled="{Binding IsMediaActive, Converter={StaticResource InverseBooleanConverter}}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ÔÑíØ ÇáÊÍßã ÇáÓÝáí: ÃÒÑÇÑ ÅÏÇÑÉ ÇáæÓÇÆØ (ãÑÈæØÉ ÈÕáÇÍíÉ ÇáãÖíÝ IsHost) æÇáãÛÇÏÑÉ -->
        <DockPanel Grid.Row="3" Margin="0,18,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#FCA5A5" TextWrapping="Wrap" VerticalAlignment="Center" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="ÈÏÁ ÇáãÍÇÏËÉ" Style="{StaticResource PrimaryButton}"
                        Command="{Binding StartMediaCommand}"
                        IsEnabled="{Binding IsHost}" />
                <Button Content="ÅíÞÇÝ ÇáæÓÇÆØ" Style="{StaticResource SecondaryButton}"
                        Command="{Binding StopMediaCommand}"
                        IsEnabled="{Binding IsMediaActive}" />
                <Button Content="ãÛÇÏÑÉ ÇáÛÑÝÉ" Style="{StaticResource DangerButton}"
                        Command="{Binding LeaveRoomCommand}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>