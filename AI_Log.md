# AI Usage Log

## Entry 01

### Date
2026-09-23

### Student
حمدي

### Tool
Claude (Anthropic)

### Purpose
صياغة مسودة أولية لوثيقة SRS-Mini، وIssues، وUser Stories، وAcceptance Criteria، وEdge Cases، وREADME.md لمشروع VideoCallSystem.

### Prompt Summary
طلبنا من الأداة إعداد مسودة SRS-Mini وIssues وREADME بناءً على الوصف الفعلي للمشروع.

### AI Suggestions
- ثمانية متطلبات وظيفية (FR-01 إلى FR-08) مبنية على ميزات المشروع الفعلية.
- خمسة متطلبات غير وظيفية (NFR-01 إلى NFR-05).
- سبع Issues كاملة.
- قالب README.md مختصر.

### Accepted Suggestions
- جميع المتطلبات الوظيفية وغير الوظيفية.
- Issues أرقام 1 إلى 7.
- قالب README.md.

### Rejected Suggestions
لا يوجد رفض مباشر.

### Reason for Rejection
لا ينطبق.

### Human Review
راجع الطالب المخرجات وتأكد من توافق الوصف مع المشروع.

---

## Entry 02

### Date
2026-09-24

### Student
هشام زيد الأديب

### Tool
Gemini (Google)

### Purpose
إنجاز وتطبيق المهمة الأولى (Issue #1: تسجيل الدخول باستخدام اسم المستخدم).

### Prompt Summary
- طلب إضافة تعليقات عربية احترافية لكود LoginViewModel.cs.
- طلب تنقية وتصفية ملفات الرسائل المشتركة لتشمل أكواد المصادقة والأخطاء الخاصة بالمهمة فقط.

### AI Suggestions
- توثيق كود LoginViewModel.cs بتعليقات عربية.
- حصر محتويات MessageType.cs و Payloads.cs في نطاق المصادقة لضمان Clean PR.

### Accepted Suggestions
- قبول التعليقات المضافة.
- قبول هيكلية الملفات المشتركة المخصصة للمهمة #1.

### Rejected Suggestions
لا يوجد رفض.

### Reason for Rejection
لا ينطبق.

### Human Review
قام الطالب باختبار بناء المشروع وتشغيل نافذة تسجيل الدخول محلياً والتأكد من عمل الواجهة بشكل سليم قبل الرفع.

---

# AI Usage — FR-03 using manus

1. تم استخدام الذكاء الاصطناعي لتحليل المشروع المرجعي وتحديد الملفات والأجزاء المتعلقة بإرسال طلب المكالمة فقط.
2. ساعد الذكاء الاصطناعي في اقتراح كود CallRequest، وحالة انتظار الرد، ومنع الطلبات المتكررة، ومعالجة الخطأ والمهلة.
3. ساعد أيضاً في تصحيح واجهة XAML وشرح أخطاء الأنواع، مثل الفرق بين ErrorPayload و CallErrorPayload.
4. تمت مراجعة الاقتراحات وتعديلها لتتوافق مع بنية المشروع ونطاق FR-03، ولم يعتمد أي كود دون مراجعة.

**الاسم:** حمدي جربان




**الاسم:** هشام الأديب
**الاسم:** حمدي جربان# تقرير إنجاز المرحلة الأولى: تأسيس البنية التحتية لتطبيق العميل (VideoCall.Client Core Setup)

## 📌 ملخص التنفيذ
تم بححمد الله إنجاز وتثبيت البنية الأساسية المعمارية (Architecture & Core Infrastructure) لمشروع العميل (`VideoCall.Client`) وفقاً للمعايير الهندسية وأنماط التصميم المطلوبة، لضمان فصل المسؤوليات وقابلية التوسع والاختبار.

---

## 🛠️ تفاصيل المهام والملفات المنجزة

1. **إعداد نقطة بداية التطبيق وإدارة النوافذ (Composition Root):**
   - تطوير `App.xaml` و `App.xaml.cs` لتعمل كنقطة مركزية لإنشاء المكونات (Composition Root).
   - تطبيق مبدأ **عكس تبعية الكائنات (Dependency Inversion Principle)** بحيث يُنشأ اتصال الشبكة `NetworkClient` مرة واحدة ويتم حقنه عبر الواجهات في جميع النوافذ والـ ViewModels دون إنشاء كائنات عشوائية.

2. **بناء واجهات التحكم والعقود الهندسية (Contracts):**
   - اعتماد واجهة `INetworkClient.cs` لتقسيم عمليات قناة التحكم (TCP) وإخفاء تعقيدات السوكيت والربط الجسدي عن واجهة المستخدم.
   - اعتماد واجهة `IMediaTransport.cs` لإدارة نقل الصوت والفيديو عبر بروتوكول (UDP) بدقة وبدون أنماط تعقيدية غير مبررة (Factory Pattern) حفاظاً على كفاءة الأداء.

3. **تأسيس معمارية الربط ونمط التصميم (MVVM Core & Commands):**
   - بناء كلاس `ViewModelBase` مطبقاً لواجهة `INotifyPropertyChanged` لتحديث واجهة المستخدم تلقائياً عند تغيير البيانات.
   - تطوير أوامر مخصصة آمنة مثل `AsyncCommand` (لمنع التكرار المزدوج وإدارة حالات التحميل والأخطاء) و `RelayCommand` للأوامر المتزامنة.

4. **تصميم واجهة المستخدم الموحدة (UI Theme & Converters):**
   - تصميم نظام ألوان ومظاهر داكنة احترافية في `Theme.xaml` يدعم بشكل كامل اتجاه الكتابة من اليمين لليسار (`RightToLeft`) للغة العربية.
   - تطوير مجموعة شاملة من المحولات (`Converters.cs`) مثل `BooleanToVisibilityConverter` و `MuteLabelConverter` لربط حالات الاتصال والميكروفون والكاميرا ديناميكياً بالعناصر المرئية.

---

## 🔍 التوثيق البرمجي والالتزامات (Git Commits)
- تم اعتماد تعليقات هندسية واضحة ومباشرة داخل الكود توضح الأنماط المستخدمة (Dependency Inversion & Observer Pattern).
- توثيق العمل ورفعه عبر نظام إدارة الإصدارات (Git) بالالتزام المعياري:
  `feat(client): تجهيز البنية الأساسية، قاعدة MVVM، واجهات الاتصال الشبكي، وتصميم واجهة المستخدم`



  **الاسم:** هشام الأديب

  ## 🚀 تقرير إنجاز الدفعة الثانية: خدمات الاتصال الشبكي (Networking Services)

- **تطوير `NetworkClient.cs`:** 
  - إدارة اتصال التحكم (TCP) مع الخادم عبر منفذ الشبكة المشترك.
  - تنفيذ حلقة قراءة خلفية (`ReadLoopAsync`) لاستقبال الرسائل والطلبات باستمرار.
  - توجيه الأحداث واستقبالها بأمان على خيط واجهة المستخدم باستخدام `Dispatcher`.

- **تطوير `UdpMediaClient.cs`:** 
  - تأسيس نقل وسائط الصوت والفيديو عبر بروتوكول (UDP) السريع.
  - تطبيق تقنية تجزئة الحزم الكبيرة (`MaxSafeUdpPayload`) لضمان عدم تجاوز الحد الآمن للنقل.
  - إضافة حلقة نبضات الحياة (`Heartbeat`) للحفاظ على استقرار جلسة الوسائط المفتوحة.

- **التوثيق وإدارة الإصدارات:** 
  - إضافة تعليقات هندسية عربية دقيقة ومختصرة على الدوال الحرجة.
  - تجهيز الملفات لعمل الـ Commit والرفع عبر Git.



   **الاسم:** هشام الأديب
  ## الدفعة الثالثة: ميزة تسجيل الدخول (Login Feature) - Issue #19
**الوصف:** ربط الواجهة الرسومية بخدمات الشبكة لإنشاء نظام تسجيل دخول متكامل، يعالج مدخلات المستخدم ويستجيب لردود الخادم (TCP).

**الملفات التي تم تطويرها/تعديلها:**
1. **`ViewModels/LoginViewModel.cs`**:
   - إدارة حالة الحقول (Server, Username, Password) وتحديث واجهة المستخدم `IsBusy` و `Status`.
   - إضافة دالة `LoginAsync` للتحقق من المدخلات، فتح الاتصال إن لزم، وإرسال البيانات.
   - الاشتراك في حدث `LoginResponseReceived` ومعالجة الرد عبر خيط الـ UI (`Dispatcher`) للتعامل الآمن مع الواجهة.
2. **`Views/LoginWindow.xaml`**:
   - تصميم واجهة عصرية بتقسيم `Grid` مرن (شعار ومعلومات التطبيق يمينًا، نموذج الإدخال يسارًا).
   - استخدام الـ `Data Binding` لربط الحقول بـ `LoginViewModel`.
3. **`Views/LoginWindow.xaml.cs`**:
   - تمرير الـ `INetworkClient` إلى الـ `ViewModel`.
   - معالجة الضغط على زر تسجيل الدخول وتمرير `PasswordBox.Password` بأمان وتفريغه فوراً.
   - فتح `MainWindow` وإغلاق نافذة الدخول عند استلام حدث `LoginSucceeded`.
=======
## Entry 03

### Date

2026-09-26

### Student

ياسر محمد السري

### Tool

ChatGPT (OpenAI)

### Purpose

المساهمة في تطوير وتجهيز الجزء المشترك من مشروع VideoCallSystem، وتحديداً مجلد VideoCall.Shared، بما يشمل بناء وتنظيم ملفات الرسائل والنماذج وطبقة الشبكات المشتركة المستخدمة بين أجزاء النظام، بالإضافة إلى توثيقها بصورة أكاديمية.

### Prompt Summary

- طلب المساعدة في تصميم وتجهيز ملفات مجلد VideoCall.Shared وفق متطلبات المشروع وبنية النظام المتفق عليها.

- طلب إضافة تعليقات عربية أكاديمية توضح وظيفة الأكواد والعناصر البرمجية دون التأثير على منطق التنفيذ.

- شملت عملية التطوير والتجهيز ملفات Messages وModels وNetworking ضمن VideoCall.Shared.

- تم كذلك تطوير وتجهيز وتوثيق ملف NetworkConfig.cs الخاص بإعدادات منافذ الاتصال الشبكي.
### AI Suggestions

- اقتراح تعليقات توضيحية باللغة العربية للعناصر البرمجية والوظائف الرئيسية.

- توضيح دور الرسائل المشتركة وأنواع الرسائل والـ Payloads المستخدمة في التواصل بين أجزاء النظام.

- المساعدة في توضيح وتصميم النماذج المشتركة مثل CallSession وConversation وRoom وUser وOnlineUser.

- توضيح آلية التعامل مع الرسائل عبر TCP من خلال TcpMessageReaderWriter، وآلية التعامل مع حزم الوسائط من خلال MediaPacket.

- توضيح إعدادات مشروع VideoCall.Shared في ملف VideoCall.Shared.csproj.

- توضيح وظيفة إعدادات الاتصال الشبكي في ملف NetworkConfig.cs، بما في ذلك منافذ TCP وUDP المستخدمة في النظام.

### Accepted Suggestions

- قبول التعليقات والتوثيق العربي المقترح للعناصر البرمجية.

- الاستفادة من المقترحات المتعلقة بتنظيم وتوثيق ملفات Messages وModels وNetworking.

- الاستفادة من الشروحات المتعلقة بوظائف NetworkConfig.cs وإعدادات الاتصال الشبكي.

- تنفيذ وتجهيز الأكواد ضمن بنية المشروع ومتطلباته، مع مراجعة المقترحات وتكييفها بما يتوافق مع تصميم النظام.

### Rejected Suggestions

لا يوجد رفض مباشر.

### Reason for Rejection

لا ينطبق.

### Human Review

قام الطالب بمراجعة جميع المقترحات وفهم وظائف الأكواد قبل اعتمادها، كما قام بتجهيز وتنفيذ مكونات الجزء المشترك ضمن مشروع VideoCall.Shared. وتم التأكد من توافق الملفات مع متطلبات المشروع وبنية النظام، ثم تجهيزها ورفعها ضمن فرع الطالب وإضافتها إلى طلب الدمج الخاص بالجزء المشترك.

# AI Usage — Shared Component (VideoCall.Shared)

تم استخدام الذكاء الاصطناعي للمساعدة في تطوير وتوثيق الجزء المشترك من المشروع، والذي يشمل مجلدات Messages وModels وNetworking.

ساعد الذكاء الاصطناعي في شرح وتوثيق العناصر البرمجية وإضافة تعليقات عربية أكاديمية توضح وظيفة الرسائل وأنواعها والـ Payloads والنماذج المشتركة وآليات الاتصال الشبكي.

تمت الاستفادة من الذكاء الاصطناعي في شرح آلية TcpMessageReaderWriter الخاصة بقراءة وكتابة الرسائل عبر TCP، بالإضافة إلى MediaPacket الخاصة بتجهيز حزم الوسائط وإرسالها.

كما تمت إضافة وتجهيز ملف NetworkConfig.cs، والذي يحتوي على إعدادات منافذ الاتصال المستخدمة في قنوات TCP وUDP الخاصة بالنظام.

تمت مراجعة جميع الاقتراحات من قبل الطالب، واتخاذ القرارات المتعلقة ببنية وتنفيذ الأكواد بما يتوافق مع متطلبات المشروع. اقتصر استخدام الذكاء الاصطناعي على المساعدة في الفهم والتصميم والتوثيق وشرح الأكواد، بينما تم تطوير وتجهيز مكونات الجزء المشترك ضمن عمل الطالب ورفعها إلى المستودع من خلال فرعه المخصص.

**الاسم:** ياسر محمد السري


# Entry 04: توثيق ومراجعة وحدة تسجيل الدخول والمصادقة (VideoCall.Server)

## 📌 البيانات الأساسية (Metadata)

| العنصر | التفاصيل |
| :--- | :--- |
| **رقم التوثيق (Entry)** | Entry 04 |
| **التاريخ (Date)** | 2026-09-26 |
| **الطالب / المطور (Student)** | حمدي (Hamdi) |
| **الأداة المستخدمة (Tool)** | Manus (AI Assistant) |
| **الهدف (Purpose)** | تحليل وتنظيم الملفات المتعلقة بتسجيل الدخول والمصادقة في مشروع VideoCallSystem، وتجهيز وصف واضح لها بهدف رفعها إلى GitHub وطلب مراجعة الكود. |

---

## 📝 ملخص التوجيهات (Prompt Summary)

<<<<<<< HEAD
معايير فنية مطبقة: الحماية من تسرب الذاكرة (IDisposable)، أمان تحديثات الواجهة (Dispatcher)، ومنع تكرار إرسال الطلبات (Interlocked).


---

## Entry 05

### Date
2026-09-26

### Student
هشام زيد الأديب

### Tool
Gemini (Google)

### Purpose
إنجاز وتوثيق الدفعة الخامسة (محرك معالجة الوسائط Media Engine) لمشروع العميل بدقة واحترافية.

### Prompt Summary
- إضافة تعليقات هندسية عربية مركزة على كافة ملفات محرك الوسائط (`VideoCaptureService`, `VideoFrameReassembler`, `AudioCaptureService`, `AudioPlaybackService`, `AudioChunkReassembler`, `AcousticEchoCanceller`, `EchoReferenceBuffer`, `FrameCodec`).
- الحفاظ التام على هيكلية الأكواد، والخوارزميات، وإدارة الذاكرة دون فقدان أي جزء برمجي.

### AI Suggestions
- توثيق آليات التقاط ومعالجة الفيديو والصوت.
- توضيح دور مرشح إلغاء الصدى الصوتي (AEC) والذاكرة الدائرية (`EchoReferenceBuffer`).
- شرح عمليات تجميع حزم UDP للإطارات والقطع الصوتية.

### Accepted Suggestions
- قبول كافة التعليقات الهندسية والتنظيمية المضافة بدقة عالية لتسهيل المراجعة.

### Rejected Suggestions
لا يوجد رفض.

### Reason for Rejection
لا ينطبق.

### Human Review
قام الطالب بمراجعة جميع الأكواد الموثقة، والتأكد من سلامة بنيتها ومطابقتها التامة لمعايير المشروع والأداء قبل اعتمادها ورفعها للمستودع.

# AI Usage — Phase 5 (Media Engine)

تم استخدام الذكاء الاصطناعي لمساعدة الطالب في توثيق وهندسة مكونات الدفعة الخامسة الخاصة بمحرك معالجة الوسائط (الصوت والفيديو وإلغاء الصدى وتقطيع الحزم). ساعدت الأداة في إضافة تعليقات توضيحية دقيقة وموجزة تشرح عمل الخوارزميات الحساسة (مثل NLMS والأجزاء والمخازن الدائرية) مع الحفاظ الكامل على الأكواد الأصلية للعميل دون أي تغيير بنيوي. قام الطالب بالمراجعة والاعتماد النهائي.


**الاسم:** هشام زيد الأديب

Entry 06
Date
2026-09-26

Student
هشام زيد الأديب

Tool
Gemini (Google)

Purpose
إنجاز وتوثيق الدفعة السادسة والأخيرة (واجهات المكالمات والتوثيق Calls UI & Polish) لمشروع العميل بدقة واحترافية.

Prompt Summary
إضافة تعليقات برمجية مركزة ومهمة على ملفات نماذج العرض (CallViewModel, GroupCallViewModel, RemoteParticipantViewModel).

توثيق ملفات واجهات المستخدم (XAML) والأكواد الخلفية (Code-Behind) لنوافذ المكالمات (CallWindow, GroupCallWindow, IncomingCallWindow).

الحفاظ التام على بنية الكود، وعناصر التصميم، وآليات ربط البيانات (Data Binding) دون أي فقد أو أخطاء.

AI Suggestions
شرح هيكلة تصميم واجهات XAML (مثل تقسيم Grid وعرض الفيديو باستخدام Picture-in-Picture).

توضيح آلية ربط البيانات بين واجهات العرض (Views) ونماذج العرض (ViewModels).

توثيق تقنيات الحماية من تعدد المسارات (Thread-Safety) عند التعامل مع أحداث واجهة المستخدم (مثل استخدام Interlocked لقبول/رفض المكالمة).

Accepted Suggestions
قبول كافة التعليقات التوضيحية المضافة للأجزاء المهمة فقط لضمان بقاء الكود نظيفاً وسهل الفهم والمراجعة.

Rejected Suggestions
لا يوجد رفض.

Reason for Rejection
لا ينطبق.
=======
- طلب فحص مشروع `VideoCall.Server` وتحديد جميع الملفات المرتبطة بتسجيل الدخول.
- طلب تحليل تدفق المصادقة من استقبال اتصال TCP حتى نجاح تسجيل الدخول أو رفضه.
- طلب فصل الملفات المباشرة الخاصة بتسجيل الدخول عن الملفات الداعمة لباقي وظائف المكالمات والحضور.
- طلب إعداد نص عربي واضح لرسالة الالتزام (Commit) وطلب مراجعة الكود (Pull Request).

---

## 💡 اقتراحات الذكاء الاصطناعي (AI Suggestions)

- تحديد `ProtocolRouter.cs` كنقطة تنفيذ طلب `LoginRequest`.
- تحديد `ICredentialValidator.cs` و `DevelopmentCredentialValidator.cs` كمسؤولي التحقق من بيانات الدخول.
- تحديد `ClientSession.cs` و `IClientHandler.cs` لإدارة حالة المصادقة و `SessionToken`.
- تحديد `IProtocolMessageDispatcher.cs` لتمرير رسائل TCP إلى منطق تسجيل الدخول.
- التنبيه إلى وجود كلمات مرور تجريبية مكتوبة بشكل مباشر داخل `Program.cs`.
- اقتراح مراجعة أمان كلمات المرور، وتخزينها باستخدام Hash، واستخدام متغيرات البيئة أو قاعدة بيانات قبل الإنتاج.

### ✅ الاقتراحات المقبولة (Accepted Suggestions)
- اعتماد قائمة الملفات المباشرة الخاصة بتسجيل الدخول.
- اعتماد تحليل تدفق تسجيل الدخول والتحقق من حالات النجاح والفشل وتسجيل الدخول المتكرر.
- اعتماد التنبيه الأمني المتعلق بكلمات المرور النصية وغياب آليات الحماية الإنتاجية.
- اعتماد صياغة تعليق عربي يطلب مراجعة الكود.

### ❌ الاقتراحات المرفوضة وسبب الرفض (Rejected Suggestions & Reason)
- **الرفض المباشر:** لا يوجد رفض مباشر.
- **سبب الرفض:** لا ينطبق.

---

## 🔍 المراجعة البشرية الأوليّة (Human Review)

تمت مراجعة التحليل والاقتراحات من قبل الطالب، وتم اختيار الملفات المتعلقة بتسجيل الدخول فقط قبل تجهيزها للرفع إلى GitHub. تم التأكد من أن الاقتراحات متوافقة مع بنية المشروع ومتطلبات المهمة، ولم يتم اعتماد أي كود أو توصية دون مراجعة وفهم بشري.

---

## 📋 بيان إسناد مهمة تسجيل الدخول والمصادقة

> **تنويه صريح بالإسناد:**
> - **اسم المهمة:** بناء وحدة تسجيل الدخول والمصادقة (`VideoCall.Server`)
> - **المطور المسند إليه:** حمدي (Hamdi)
> - **الجهة المنفذة لكتابة الكود:** الذكاء الاصطناعي (AI Assistant - Manus)
> - **المرجع الفني:** عقود ومواصفات `VideoCall.Shared`
>
> **بيان الإسناد:**
> قام الذكاء الاصطناعي ببناء وكتابة الأكواد البرمجية الخاصة بتسجيل الدخول في مشروع `VideoCall.Server` بشكل كامل. اعتمد الذكاء الاصطناعي في هندسة وتأطير الكود على الموديلات والـ DTOs والمحولات المعرّفة في `VideoCall.Shared` (مثل `NetworkMessage` و `TcpMessageFraming` و `CommandType`). تمثل دور المطور البشري في توجيه الذكاء الاصطناعي، ومراجعة الأكواد المولدة، واختبار توافقها، واعتماد رفعها إلى المستودع.

---

## 🔄 ما تم قبوله وما تم تعديله من مخرجات الذكاء الاصطناعي

### 1. الاقتراحات التي تم قبولها
- اعتماد بنية وحدة تسجيل الدخول داخل مشروع `VideoCall.Server`.
- استخدام `ProtocolRouter` لمعالجة رسائل تسجيل الدخول الواردة عبر TCP.
- استخدام `ICredentialValidator` كواجهة مستقلة للتحقق من اسم المستخدم وكلمة المرور.
- استخدام `DevelopmentCredentialValidator` للتحقق من بيانات الحسابات التجريبية.
- استخدام `ClientSession` و `IClientHandler` لإدارة هوية المستخدم وحالة المصادقة.
- منع تسجيل الدخول مرة أخرى إذا كانت الجلسة مصادقاً عليها مسبقاً.
- منع دخول المستخدم نفسه أكثر من مرة في الوقت نفسه.
- إنشاء `SessionToken` عند نجاح تسجيل الدخول وإرساله ضمن رسالة الاستجابة.
- إرسال استجابة نجاح أو فشل واضحة للعميل بعد محاولة تسجيل الدخول.
- فصل مسؤولية التحقق من بيانات الدخول عن مسؤولية إدارة المستخدمين المتصلين.
- إضافة تعليقات وتوثيق يشرحان وظيفة عناصر المصادقة وتدفقها.

### 2. التعديلات والتكييفات التي أجراها المطور (Developer Adaptations)
- تكييف أسماء الأنواع والرسائل لتتوافق مع العقود الفعلية الموجودة في `VideoCall.Shared`، مثل `MessageType.LoginRequest` و `LoginRequestPayload` و `LoginResponsePayload`.
- تعديل طريقة تمرير الرسائل لتتوافق مع البنية الحالية، حيث تمر الرسائل من `ClientSession` إلى `IProtocolMessageDispatcher` ثم إلى `ProtocolRouter`.
- تعديل التحقق من اسم المستخدم ليشمل إزالة المسافات، ورفض الاسم الفارغ، وتحديد الحد الأقصى لطول الاسم إلى 64 حرفاً.
- تعديل استجابات الخطأ لتستخدم الأكواد المعرفة في المشروع مثل `InvalidCredentials` و `AlreadyLoggedIn`.
- تكييف إدارة الجلسة مع واجهة `IClientHandler`، بحيث لا يتم اعتماد المصادقة إلا بعد نجاح التحقق وإضافة المستخدم إلى قائمة الحضور.
- تعديل معالجة قطع الاتصال لإزالة المستخدم من قائمة المتصلين وتنظيف الجلسة بصورة صحيحة.
- مراجعة التعليقات والتوثيق وتعديلها لتتوافق مع أسلوب المشروع ومتطلبات المهمة.
- استبعاد ملفات المكالمات والوسائط والـ API غير المرتبطة مباشرة بوحدة تسجيل الدخول من عملية الرفع الخاصة بهذه المهمة.

### 3. ما لم يتم اعتماده كما هو
- لم يتم اعتماد أي اقتراح يخالف بنية المشروع أو العقود المعرفة في `VideoCall.Shared`.
- لم يتم اعتماد كلمات المرور النصية كحل مناسب للإنتاج؛ فهي موجودة حالياً لأغراض التطوير والاختبار فقط.
- لم يتم اعتبار الحل الحالي جاهزاً للإنتاج قبل إضافة تخزين آمن لكلمات المرور باستخدام Hash، وتأمين النقل، وإضافة حماية من محاولات الدخول المتكررة.

---

## 🎯 المراجعة البشرية النهائية (Final Human Review)

راجع المطور **حمدي** المخرجات، وقارنها ببنية المشروع وعقود `VideoCall.Shared`، ثم عدّل ما يلزم ليتوافق مع أسماء الأنواع ومسار الرسائل وقواعد المصادقة المطلوبة. كما تم تحديد الملفات التي تخص تسجيل الدخول فقط قبل رفعها إلى المستودع، ولم يتم اعتماد الكود أو رفعه دون مراجعة تفصيلية وفهم كامل.
>>>>>>> origin/main
