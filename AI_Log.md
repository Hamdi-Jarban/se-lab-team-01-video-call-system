# AI Usage Log

## سجل استخدام الذكاء الاصطناعي وتوزيع المهام

> **المشروع:** نظام الاتصال المباشر عبر الفيديو — `VideoCallSystem`  
> **تاريخ التوثيق:** سبتمبر 2026  
> **أداة الذكاء الاصطناعي المستخدمة في التخطيط ومهام الخادم:** Manus (AI Assistant)  
> **الغرض من المستند:** توثيق استخدام الذكاء الاصطناعي، والاقتراحات المقدمة، وما تم قبوله أو تعديله، والمراجعة البشرية ضمن مراحل تطوير المشروع.

---

# 1. دليل تقسيم العمل وإدارة الفريق

## 1.1 البيانات الأساسية

| العنصر | التفاصيل |
|---|---|
| اسم المشروع | نظام الاتصال المباشر عبر الفيديو (`VideoCallSystem`) |
| نوع المستند | دليل تقسيم العمل وإدارة الشراكة (`Work Breakdown & Team Governance`) |
| المطور المسؤول عن التخطيط | حمدي (`Hamdi`) — اقتراح المعمارية وتوزيع المهام بمساعدة الذكاء الاصطناعي |
| أعضاء الفريق | ياسر (`Yasser`): `VideoCall.Shared`<br>هشام (`Hesham`): `VideoCall.Client`<br>حمدي (`Hamdi`): `VideoCall.Server` |
| أداة الذكاء الاصطناعي | Manus (AI Assistant) |
| تاريخ التوثيق | سبتمبر 2026 |
| مبدأ المراجعة | مراجعة بشرية ومراجعة زميل قبل الدمج (`Four-Eyes Principle`) |

## 1.2 الهدف من استخدام الذكاء الاصطناعي في هيكلة العمل

تم استخدام الذكاء الاصطناعي للمساعدة في تفكيك نظام الاتصال إلى مشاريع برمجية مستقلة وغير متداخلة (`Decoupled Projects`) بهدف:

- تقليل تعارضات الدمج (`Merge Conflicts`) من خلال تقسيم حزم العمل حسب المشاريع الثلاثة الرئيسية: `Shared` و`Client` و`Server`.
- تحقيق استقلالية التطوير وتوزيع المسؤوليات بوضوح بين أعضاء الفريق.
- تحديد معايير الاستلام (`Acceptance Criteria`) لكل مهمة قبل البدء في التنفيذ.
- تنظيم الفروع وطلبات السحب (`Pull Requests`) بما يدعم المراجعة البشرية قبل الدمج.

## 1.3 مصفوفة توزيع المهام والأدوار

| العضو | المشروع المسند | نطاق العمل والمسؤوليات | نواتج التسليم |
|---|---|---|---|
| ياسر (`Yasser`) | `VideoCall.Shared` | تصميم كائنات نقل البيانات المشتركة، وبناء التأطير الشبكي، وتحديد أنواع الأوامر والرسائل | `VideoCall.Shared.csproj`، مكتبة العقود والـ DTOs، بروتوكولات التسلسل والتأطير |
| هشام (`Hesham`) | `VideoCall.Client` | بناء واجهات العميل باستخدام WPF وMVVM، وتطوير الاتصال الشبكي والوسائط، وتطبيق التحديث الآمن للواجهة | `VideoCall.Client.csproj`، واجهة تسجيل الدخول، مكونات الغرف والدعوات، برامج تشغيل الشبكة والوسائط |
| حمدي (`Hamdi`) | `VideoCall.Server` | بناء خادم TCP/UDP، وتصميم المصادقة وتوجيه الرسائل وإدارة الجلسات، وإدارة المراجعات والتوثيق | `VideoCall.Server.csproj`، بنية المصادقة والجلسات، سجلات التوثيق والمراجعة |

## 1.4 دورة حياة المهمة وسياسة المراجعة

```text
إنشاء Issue ومعايير القبول
          ↓
إنشاء فرع للميزة
          ↓
التحليل والتنفيذ بمساعدة الذكاء الاصطناعي
          ↓
تكييف الكود ومراجعته بشرياً
          ↓
مراجعة زميل عبر Pull Request
          ↓
الموافقة والدمج
```

---

# 2. سجلات استخدام الذكاء الاصطناعي

## Entry 01 — التخطيط وتحليل المتطلبات

| العنصر | التفاصيل |
|---|---|
| التاريخ | 2026-09-23 |
| الطالب | حمدي |
| الأداة | Claude (Anthropic) |
| الهدف | إعداد مسودة أولية لوثيقة `SRS-Mini` و`Issues` و`User Stories` و`Acceptance Criteria` و`Edge Cases` و`README.md` لمشروع `VideoCallSystem` |

### ملخص الطلب

طُلب من الأداة إعداد مسودة للمتطلبات والـ Issues وملف README بناءً على الوصف الفعلي للمشروع.

### اقتراحات الذكاء الاصطناعي

- ثمانية متطلبات وظيفية (`FR-01` إلى `FR-08`).
- خمسة متطلبات غير وظيفية (`NFR-01` إلى `NFR-05`).
- سبع Issues كاملة.
- قالب مختصر لملف `README.md`.

### الاقتراحات المقبولة

- اعتماد المتطلبات الوظيفية وغير الوظيفية بعد مراجعتها.
- اعتماد Issues من 1 إلى 7.
- الاستفادة من قالب `README.md`.

### الاقتراحات غير المعتمدة

لا يوجد رفض مباشر.

### المراجعة البشرية

راجع الطالب المخرجات وتأكد من توافقها مع وصف المشروع قبل اعتمادها.

---

## Entry 02 — واجهة تسجيل الدخول في العميل

| العنصر | التفاصيل |
|---|---|
| التاريخ | 2026-09-24 |
| الطالب | هشام زيد الأديب |
| الأداة | Gemini (Google) |
| الهدف | إنجاز وتطبيق المهمة الأولى (`Issue #1`) الخاصة بتسجيل الدخول باستخدام اسم المستخدم |

### ملخص الطلب

- إضافة تعليقات عربية احترافية إلى `LoginViewModel.cs`.
- تنقية ملفات الرسائل المشتركة لتشمل أكواد المصادقة والأخطاء الخاصة بالمهمة فقط.

### اقتراحات الذكاء الاصطناعي

- توثيق `LoginViewModel.cs` بتعليقات عربية.
- حصر محتويات `MessageType.cs` و`Payloads.cs` في نطاق المصادقة لضمان طلب سحب واضح ومحدود.

### الاقتراحات المقبولة

- قبول التعليقات المضافة بعد مراجعتها.
- قبول هيكلية الملفات المشتركة المخصصة للمهمة.

### الاقتراحات غير المعتمدة

لا يوجد رفض مباشر.

### المراجعة البشرية

قام الطالب ببناء المشروع وتشغيل نافذة تسجيل الدخول محلياً، ثم تحقق من عمل الواجهة قبل رفع التغييرات.

---

## سجل استخدام الذكاء الاصطناعي — FR-03: طلب المكالمة الخاصة

تم استخدام الذكاء الاصطناعي في هذه المهمة من أجل:

1. تحليل المشروع المرجعي وتحديد الملفات المتعلقة بإرسال طلب المكالمة فقط.
2. اقتراح آلية `CallRequest` وحالة انتظار الرد.
3. اقتراح منع الطلبات المتكررة ومعالجة الخطأ والمهلة الزمنية.
4. المساعدة في تصحيح واجهة XAML وشرح الفرق بين `ErrorPayload` و`CallErrorPayload`.

تمت مراجعة الاقتراحات وتعديلها بما يتوافق مع بنية المشروع ونطاق `FR-03`، ولم يتم اعتماد أي كود دون مراجعة بشرية.

**الأسماء المرتبطة بالمهمة:**

- حمدي جربان.
- هشام الأديب.

---

# 3. تقارير إنجاز مكونات العميل

## 3.1 المرحلة الأولى — تأسيس البنية التحتية لـ `VideoCall.Client`

### ملخص التنفيذ

تم تجهيز البنية الأساسية المعمارية لمشروع العميل `VideoCall.Client` وفق مبادئ فصل المسؤوليات وقابلية التوسع والاختبار.

### الأعمال والملفات المنجزة

1. **نقطة بداية التطبيق وإدارة النوافذ (`Composition Root`)**
   - تجهيز `App.xaml` و`App.xaml.cs` كنقطة مركزية لإنشاء المكونات.
   - تطبيق مبدأ عكس التبعية (`Dependency Inversion Principle`).
   - إنشاء اتصال `NetworkClient` مرة واحدة وحقنه عبر الواجهات في النوافذ و`ViewModels`.

2. **عقود التحكم والاتصال**
   - اعتماد `INetworkClient.cs` لعزل عمليات TCP عن واجهة المستخدم.
   - اعتماد `IMediaTransport.cs` لإدارة نقل الصوت والفيديو عبر UDP.

3. **بنية MVVM والأوامر**
   - بناء `ViewModelBase` المطبق لواجهة `INotifyPropertyChanged`.
   - تطوير `AsyncCommand` لمنع التكرار وإدارة التحميل والأخطاء.
   - تطوير `RelayCommand` للأوامر المتزامنة.

4. **المظهر والمحولات**
   - تصميم `Theme.xaml` بمظهر داكن ودعم اتجاه الكتابة من اليمين إلى اليسار.
   - تطوير محولات مثل `BooleanToVisibilityConverter` و`MuteLabelConverter`.

### التوثيق وإدارة الإصدارات

- إضافة تعليقات هندسية توضّح الأنماط المستخدمة.
- توثيق العمل بالالتزام المعياري التالي:

```text
feat(client): تجهيز البنية الأساسية، قاعدة MVVM، واجهات الاتصال الشبكي، وتصميم واجهة المستخدم
```

**الاسم:** هشام الأديب

## 3.2 المرحلة الثانية — خدمات الاتصال الشبكي

- تطوير `NetworkClient.cs` لإدارة اتصال TCP وتنفيذ حلقة القراءة الخلفية `ReadLoopAsync`.
- توجيه الأحداث إلى خيط واجهة المستخدم باستخدام `Dispatcher`.
- تطوير `UdpMediaClient.cs` لنقل الصوت والفيديو عبر UDP.
- تطبيق تجزئة الحزم الكبيرة باستخدام `MaxSafeUdpPayload`.
- إضافة نبضات الحياة (`Heartbeat`) للحفاظ على استقرار جلسة الوسائط.
- إضافة تعليقات عربية على الدوال الحرجة وتجهيز الملفات للرفع عبر Git.

**الاسم:** هشام الأديب

## 3.3 المرحلة الثالثة — ميزة تسجيل الدخول في العميل

**المهمة:** `Issue #19 — Login Feature`

**الوصف:** ربط واجهة العميل بخدمات الشبكة لإنشاء نظام تسجيل دخول يعالج مدخلات المستخدم ويستجيب لردود الخادم عبر TCP.

### الملفات التي تم تطويرها أو تعديلها

1. **`ViewModels/LoginViewModel.cs`**
   - إدارة حقول الخادم واسم المستخدم وكلمة المرور.
   - تحديث حالتي `IsBusy` و`Status`.
   - تنفيذ `LoginAsync` للتحقق من المدخلات وفتح الاتصال وإرسال البيانات.
   - معالجة `LoginResponseReceived` عبر `Dispatcher`.

2. **`Views/LoginWindow.xaml`**
   - تصميم واجهة إدخال مرنة باستخدام `Grid`.
   - استخدام `Data Binding` لربط الحقول بـ `LoginViewModel`.

3. **`Views/LoginWindow.xaml.cs`**
   - تمرير `INetworkClient` إلى `ViewModel`.
   - تمرير كلمة المرور من `PasswordBox` وتفريغها بعد الاستخدام.
   - فتح `MainWindow` وإغلاق نافذة الدخول عند استلام `LoginSucceeded`.

---

## Entry 03 — تجهيز الجزء المشترك `VideoCall.Shared`

| العنصر | التفاصيل |
|---|---|
| التاريخ | 2026-09-26 |
| الطالب | ياسر محمد السري |
| الأداة | ChatGPT (OpenAI) |
| الهدف | تطوير وتنظيم وتوثيق الرسائل والنماذج وطبقة الشبكات المشتركة المستخدمة بين أجزاء النظام |

### ملخص العمل

- تجهيز مجلدات `Messages` و`Models` و`Networking`.
- تجهيز وتوثيق `NetworkConfig.cs` الخاص بمنافذ TCP وUDP.
- إضافة تعليقات عربية توضّح وظائف العناصر البرمجية دون تغيير منطق التنفيذ.

### اقتراحات الذكاء الاصطناعي

- توثيق العناصر والوظائف الرئيسية باللغة العربية.
- شرح أنواع الرسائل والـ Payloads المستخدمة في التواصل.
- المساعدة في تصميم نماذج مثل `CallSession` و`Conversation` و`Room` و`User` و`OnlineUser`.
- شرح `TcpMessageReaderWriter` لقراءة وكتابة رسائل TCP.
- شرح `MediaPacket` لتجهيز حزم الوسائط.
- توضيح إعدادات `VideoCall.Shared.csproj`.
- توضيح منافذ TCP وUDP في `NetworkConfig.cs`.

### الاقتراحات المقبولة

- اعتماد التعليقات والتوثيق العربي بعد المراجعة.
- الاستفادة من تنظيم وتوثيق ملفات `Messages` و`Models` و`Networking`.
- الاستفادة من شرح `NetworkConfig.cs` وآلية الاتصال.
- تكييف الأكواد مع بنية المشروع ومتطلباته.

### الاقتراحات غير المعتمدة

لا يوجد رفض مباشر.

### المراجعة البشرية

راجع الطالب جميع المقترحات وفهم وظائف الأكواد، ثم جهّز مكونات `VideoCall.Shared` ورفعها ضمن فرعه وأضافها إلى طلب الدمج الخاص بالجزء المشترك.

---

# 4. Entry 04 — وحدة تسجيل الدخول والمصادقة في `VideoCall.Server`

## 4.1 البيانات الأساسية

| العنصر | التفاصيل |
|---|---|
| رقم التوثيق | `Entry 04` |
| التاريخ | 2026-09-26 |
| الطالب / المطور | حمدي جربان (`Hamdi Jarban`) |
| الأداة | Manus (AI Assistant) |
| المشروع | `VideoCall.Server` |
| المرجع الفني | عقود ومواصفات `VideoCall.Shared` |
| الهدف | تحليل وتنظيم الملفات المتعلقة بتسجيل الدخول والمصادقة وتجهيزها للمراجعة والرفع إلى GitHub |

## 4.2 ملخص التوجيهات

- فحص مشروع `VideoCall.Server` وتحديد الملفات المرتبطة بتسجيل الدخول.
- تحليل تدفق المصادقة من استقبال اتصال TCP حتى نجاح الدخول أو رفضه.
- فصل ملفات تسجيل الدخول المباشرة عن ملفات المكالمات والحضور والوسائط.
- إعداد نص عربي واضح للـ Commit وطلب مراجعة الكود عبر Pull Request.

## 4.3 اقتراحات الذكاء الاصطناعي

- تحديد `ProtocolRouter.cs` كنقطة تنفيذ `LoginRequest`.
- تحديد `ICredentialValidator.cs` و`DevelopmentCredentialValidator.cs` للتحقق من بيانات الدخول.
- تحديد `ClientSession.cs` و`IClientHandler.cs` لإدارة المصادقة و`SessionToken`.
- تحديد `IProtocolMessageDispatcher.cs` لتمرير رسائل TCP إلى منطق تسجيل الدخول.
- التنبيه إلى وجود كلمات مرور تجريبية مكتوبة مباشرة داخل `Program.cs`.
- اقتراح استخدام Hash ومتغيرات بيئة أو قاعدة بيانات قبل استخدام النظام في الإنتاج.

## 4.4 الاقتراحات المقبولة

- اعتماد قائمة الملفات المباشرة الخاصة بتسجيل الدخول.
- اعتماد تحليل تدفق الدخول وحالات النجاح والفشل والدخول المتكرر.
- اعتماد التنبيه الأمني بشأن كلمات المرور النصية وغياب حماية الإنتاج.
- اعتماد صياغة تعليق عربي يطلب مراجعة الكود.

## 4.5 الاقتراحات غير المعتمدة وسبب الرفض

- **الرفض المباشر:** لا يوجد رفض مباشر.
- **السبب:** لا ينطبق.
- لم يتم اعتماد أي اقتراح يخالف بنية المشروع أو عقود `VideoCall.Shared`.
- لم يتم اعتبار كلمات المرور النصية حلاً مناسباً للإنتاج؛ وهي مخصصة للتطوير والاختبار فقط.
- لم يتم اعتبار الحل جاهزاً للإنتاج قبل إضافة Hash وتأمين النقل والحماية من المحاولات المتكررة.

## 4.6 بيان إسناد المهمة

> **اسم المهمة:** بناء وحدة تسجيل الدخول والمصادقة (`VideoCall.Server`)  
> **المطور المسند إليه:** حمدي جربان (`Hamdi Jarban`)  
> **الجهة المنفذة لكتابة الكود:** الذكاء الاصطناعي (`AI Assistant - Manus`)  
> **المرجع الفني:** عقود ومواصفات `VideoCall.Shared`
>
> قام الذكاء الاصطناعي ببناء وكتابة الأكواد البرمجية الخاصة بتسجيل الدخول في مشروع `VideoCall.Server`، بالاعتماد على الموديلات والـ DTOs والمحولات المعرفة في `VideoCall.Shared`. وتمثل دور المطور البشري في توجيه الذكاء الاصطناعي، ومراجعة الأكواد المولدة، واختبار توافقها، واعتماد رفعها إلى المستودع.

## 4.7 ما تم قبوله وما تم تعديله

### الاقتراحات المقبولة

- اعتماد بنية وحدة تسجيل الدخول داخل `VideoCall.Server`.
- استخدام `ProtocolRouter` لمعالجة رسائل تسجيل الدخول عبر TCP.
- استخدام `ICredentialValidator` للتحقق من اسم المستخدم وكلمة المرور.
- استخدام `DevelopmentCredentialValidator` للحسابات التجريبية.
- استخدام `ClientSession` و`IClientHandler` لإدارة هوية المستخدم وحالة المصادقة.
- منع تسجيل الدخول مرة أخرى في الجلسة المصادق عليها.
- منع دخول المستخدم نفسه أكثر من مرة في الوقت نفسه.
- إنشاء `SessionToken` عند نجاح الدخول وإرساله في الاستجابة.
- إرسال استجابة نجاح أو فشل واضحة للعميل.
- فصل التحقق من بيانات الدخول عن إدارة المستخدمين المتصلين.
- إضافة تعليقات وتوثيق لتدفق المصادقة.

### التعديلات والتكييفات التي أجراها المطور

- تكييف أسماء الرسائل والأنواع مع العقود الفعلية الموجودة في `VideoCall.Shared`، مثل `MessageType.LoginRequest` و`LoginRequestPayload` و`LoginResponsePayload`.
- تكييف مسار الرسائل مع البنية الحالية:

  ```text
  ClientSession → IProtocolMessageDispatcher → ProtocolRouter
  ```

- تعديل التحقق من اسم المستخدم ليشمل إزالة المسافات ورفض الاسم الفارغ وتحديد الحد الأقصى للطول إلى 64 حرفاً.
- استخدام أكواد الأخطاء المعرفة في المشروع، مثل `InvalidCredentials` و`AlreadyLoggedIn`.
- اعتماد المصادقة فقط بعد نجاح التحقق وإضافة المستخدم إلى قائمة الحضور.
- تنظيف قائمة المتصلين والجلسة عند قطع الاتصال.
- مراجعة التعليقات والتوثيق وتكييفهما مع أسلوب المشروع.
- استبعاد ملفات المكالمات والوسائط وواجهة API غير المرتبطة مباشرة بمهمة تسجيل الدخول من عملية الرفع الخاصة بها.

## 4.8 المراجعة البشرية النهائية

راجع المطور حمدي جربان المخرجات وقارنها ببنية المشروع وعقود `VideoCall.Shared`، ثم عدّل ما يلزم ليتوافق مع أسماء الأنواع ومسار الرسائل وقواعد المصادقة. كما تم تحديد الملفات الخاصة بتسجيل الدخول قبل رفعها إلى المستودع، ولم يتم اعتماد الكود أو رفعه دون مراجعة وفهم بشري.

---

# 5. ملاحظات التحقق والشفافية

- يوضح هذا المستند الاقتراحات المقدمة من الذكاء الاصطناعي، والاقتراحات المقبولة، والتعديلات البشرية، وما لم يتم اعتماده.
- يجب التأكد من أن أسماء الأنواع المذكورة، مثل `MessageType` و`TcpMessageReaderWriter` و`LoginRequestPayload`، مطابقة فعلاً للنسخة النهائية من `VideoCall.Shared`.
- لا ينبغي اعتبار النظام جاهزاً للإنتاج قبل معالجة كلمات المرور النصية، وتأمين النقل، وإضافة حماية من محاولات الدخول المتكررة.
- تمثل المراجعة البشرية والاختبار والموافقة مسؤولية المطور والفريق، ولا يحل استخدام الذكاء الاصطناعي محل المراجعة أو الاختبار.

---

## إقرار المراجعة

تمت مراجعة محتوى هذا السجل من قبل أعضاء الفريق المعنيين، ويهدف إلى توثيق مساهمة الذكاء الاصطناعي والقرارات البشرية المرتبطة بتطوير المشروع.
